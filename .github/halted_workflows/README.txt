This workflows encompass the CI pipeline and should be run. We already run them independently,
so we skip them to avoid wasting GitHub runner minutes. We'll activate them
again when we no longer run the CI pipeline independently.
